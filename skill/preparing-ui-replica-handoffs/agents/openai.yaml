interface:
  display_name: "UI 复刻交付准备 / UI Replica Handoff Prep"
  short_description: "前中后台共享 Shell、固定缩放评分与自适应 UI 复刻交付"
  default_prompt: "Use $preparing-ui-replica-handoffs to classify design evidence, group pages into front/middle/back/public-auth shell families, freeze one canonical navigation tree per shell, define page-content-only route contracts, lock the baseline browser zoom for replica scoring, and keep wide/narrow/zoom acceptance stability-only."
